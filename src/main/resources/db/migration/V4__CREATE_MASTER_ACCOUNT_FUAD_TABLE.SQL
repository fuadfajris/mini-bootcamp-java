--POSTGRES
--CREATE TABLE "flyway"."master_account" (
--    ID       BIGSERIAL PRIMARY KEY,
--    USER_ID  BIGINT NOT NULL,
--    BALANCE  DECIMAL(15,2) NOT NULL DEFAULT 0.00,
--    CONSTRAINT fk_master_account_user FOREIGN KEY (USER_ID) REFERENCES MASTER_USER(ID) ON DELETE CASCADE
--);

--ORACLE
CREATE TABLE MASTER_ACCOUNT_FUAD (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    USER_ID NUMBER NOT NULL,
    BALANCE NUMBER DEFAULT 0 CHECK (BALANCE >= 0),
    CONSTRAINT FK_MASTER_ACCOUNT_USER_FUAD FOREIGN KEY (USER_ID)
    REFERENCES MASTER_USER_FUAD(ID) ON DELETE CASCADE
);